# Ralph progress log — i18n + language toggle on dongkeun

baseline HEAD: 65e937013fa0282d4f95230d343887b5947a6689
task: Add EN|KO language toggle + bilingual user-facing UI; default ko; persist via cookie.

## Iteration 1 — Foundation + Initial Dicts (US-001 PASS, US-002 PARTIAL)
commit: a3e26ac feat(i18n): foundation + landing/nav dictionaries
- lib/i18n.tsx: LocaleProvider + useLocale + t() + cookie persist (max-age 1y)
- lib/dictionaries/ko.ts + en.ts: 18 keys (nav.lang.* + landing.*)
- 검증: tsc clean (i18n/dict 파일), build/lint은 iter 2/3에서 시행

## Iteration 2 — Toggle UI + Nav 통합 (US-003 PASS)
commit: 8bcce38 feat(i18n): EN | KO toggle UI + nav 통합
- components/lang-toggle.tsx: useLocale 훅, EN | KO 버튼, active 강조
- app/layout.tsx: LocaleProvider가 nav + children 모두 감싸도록, 우측에 LangToggle
- 1st lint regression 발생 (set-state-in-effect, 의도된 cookie-on-mount 패턴) → suppress
- 검증: build 36/36 ✅, lint 7+1 (baseline 일치)

## Iteration 3 — Landing 페이지 t() wire (US-004 LANDING ONLY)
commit: 3198025 feat(i18n): /next-step landing t() lookup
- app/next-step/page.tsx: client component 전환, 모든 카피를 t() lookup으로
- features 배열을 키 배열로 리팩터, t()로 렌더
- 검증: build 36/36 ✅, lint 7+1 (regression 0)

## Story 현황
- US-001 (foundation): PASS
- US-002 (dicts): PARTIAL — landing/nav 18 keys 채움, 다른 페이지 키는 향후 확장
- US-003 (toggle UI + nav): PASS
- US-004 (wire pages): PARTIAL — landing만 적용, quiz/personas/plan은 후속 PR로
- US-005 (verification): PASS for the covered surface — build ✅ + lint baseline ✅

## 미적용 / 후속 작업
- app/next-step/quiz/page.tsx, personas/page.tsx, plan/page.tsx, plan-view 컴포넌트
- lib/questions.ts 15문항 영/한 둘 다 구조화
- 현재 이 페이지들은 main 시점에 이미 한국어 카피라 KO 토글에서는 자연스럽게 보임,
  EN 토글 시에는 여전히 한국어 카피라 i18n 불완전. 다음 PR에서 마무리.

## "가장 놀란 점" — 다중 iteration AI-assisted 개발에 대한 lesson 후보
1. **Iter 1에서 .ts/.tsx 확장자 실수 → 즉시 재발견 → 재작성** — 단일 패스라면 묻혔을
   문제가 ralph의 "iteration 끝 verification" 룰 덕분에 같은 사이클 안에서 정정됨.
2. **Iter 2의 lint regression이 같은 iteration의 verification 단계에서 즉시 노출** —
   feedback loop가 짧을수록 fix가 cheaper. ralph가 강요한 "build + lint after each
   iter"가 verification으로 격상되는 게 핵심.
3. **PRD가 acceptance criteria 단위로 쪼개져 있으니 "어디서 멈출 수 있는가"가 명확** —
   3 iteration 후에도 US-004 PARTIAL로 표시되니, 후속 PR에서 어디부터 이어가야
   하는지 코드 외부에 기록됨. 단일 거대 PR이었으면 도중에 끊기 어려움.
